{
  "name": "tamafit-staff-calendar",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/local-first-app.js --bundle --format=esm --platform=browser --target=es2022 --outfile=app.bundle.js",
    "build:preview": "esbuild src/app.js --bundle --format=iife --platform=browser --target=es2022 --outfile=preview-app.js",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
